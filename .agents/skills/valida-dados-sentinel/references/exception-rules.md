# Regras de Exceção Relevantes

- `ANEXO` e `Informativo::Anexo` devem ficar fora do executivo;
- correções recorrentes devem estar persistidas no ETL;
- movimentações fora de expediente podem existir, mas não devem contaminar o indicador principal.
